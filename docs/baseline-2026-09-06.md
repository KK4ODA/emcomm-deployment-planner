# Baseline: state of the repository before modernization

**Captured:** 2026-09-06, commit `ba096c9` on `main`.
**Purpose:** record what the project looked like before the modernization branch so later changes can be judged against a known starting point.

## Repository

- Remote: `https://github.com/KK4ODA/emcomm-deployment-planner` (public, default branch `main`, no branch protection, no tags, no releases, no GitHub Actions, no repository secrets).
- 26 commits. History shows a Base44 export, a migration to Supabase and standalone hosting, then an "event log" offline architecture for tasks.
- Local working copy established at `C:\Dev\Emcomm Deployment Planner` by cloning directly into the (previously empty) directory. `origin/main` tracks `main`.
- GitHub CLI authenticated as `KK4ODA` with `repo` and `workflow` scopes; HTTPS git operations work.

## Toolchain on the development PC

| Tool | Version |
|------|---------|
| Node.js | 24.14.1 |
| npm | 11.11.0 |
| git | 2.53.0.windows.2 |
| Rust / cargo | not installed |

## Stack (as found)

- React 18 + Vite 6, JavaScript (JSX) with `checkJs` type checking via `tsc -p jsconfig.json`.
- Tailwind 3 + shadcn/ui (Radix) components under `src/components/ui` (49 files, 10 actually imported).
- TanStack Query 5, React Router 6, Supabase JS 2, Leaflet/react-leaflet, framer-motion, sonner toasts.
- `vite-plugin-pwa` with Workbox, `registerType: 'autoUpdate'`, no PWA icons.
- Supabase: 7 SQL migrations, 6 Edge Functions (all deployed and ACTIVE on project `rboklyjrdctsbarsbtdj`).
- Hosting: static Apache host (SPA rewrite in `public/.htaccess`).

## Baseline verification results

| Check | Result |
|-------|--------|
| `npm install` | OK. 25 audit findings (1 critical, 11 high, 11 moderate, 2 low), mostly transitive dev/build tooling (brace-expansion, babel, browserslist, fast-uri, axios via `@base44/sdk`). |
| `npm run dev` | Starts and serves the app (requires `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` in `.env.local`). |
| `npm run build` | Succeeds. `dist/` is 1.6 MB with a single ~1 MB JS chunk (no code splitting). |
| `npm run lint` | **33 errors**, all unused imports in `src/pages/*` (auto-fixable). Lint only covers `src/pages`, `src/components` (excluding `ui`) and `src/Layout.jsx`. |
| `npm run typecheck` | **Fails** with ~27 errors, all false positives in `ResetPassword.jsx`/`Templates.jsx` caused by `jsconfig.json` only including `src/components/**/*.js` (no `.jsx`), so shadcn components resolve as `RefAttributes<any>`. |
| Tests | None. No test runner configured. |

## Environment variables required

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

No `.env.example` existed. The README still described Base44 variables (`VITE_BASE44_APP_ID`, `VITE_BASE44_APP_BASE_URL`) that nothing reads.

## Known-good behaviour to preserve

- Email/password sign-in, sign-up, forgot/reset password (Supabase Auth).
- Cached identity so the app opens offline for up to 7 days.
- Deployments, sites (locations), categories, items with multi-call-sign assignment, drag-and-drop ordering.
- Tasks via the event log (IndexedDB outbox, ULIDs, status state machine, Realtime).
- ICS 205 form per site, deployment export (TXT) via Edge Function, templates, members/roles/invitations, ARES groups, notifications bell, avatar upload.
